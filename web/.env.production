# 【生产】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# 应用版本号
VITE_VERSION = 0.0.0

# 开发服务器端口（生产环境通常不需要，但保留以保持一致性）
VITE_PORT = 5173

# API 地址前缀
VITE_API_URL = /

# Delete console
VITE_DROP_CONSOLE = true

# 菜单模式：frontend（静态路由）| backend（后端接口）
VITE_ACCESS_MODE = frontend
